---
interface Props {
  Href: string
	CtaText: string;
}

const { Href, CtaText } = Astro.props;
---

<a href={Href} class="px-6 py-3 mt-12 max-w-max text-xl text-white transition-colors duration-300 bg-red-600 rounded-lg focus:shadow-outline hover:bg-red-700">{CtaText}</a>